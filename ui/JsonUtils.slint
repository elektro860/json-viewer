export global JsonUtils {
    pure callback is-valid-json-value(string) -> bool;
    pure callback is-valid-json-key(string) -> bool;
    callback set-value(int, string) -> bool;
    callback set-key(int, string) -> bool;
    callback toggle-fold(int);
    callback save();
}
